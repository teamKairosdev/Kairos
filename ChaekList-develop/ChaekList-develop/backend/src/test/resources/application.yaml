spring:
  datasource:
    url: jdbc:h2:mem:chaeklist-test;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
chaeklist:
  reading-room:
    notifications:
      initial-delay-ms: 3600000
